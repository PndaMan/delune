## What and why

<!-- What does this change, and why is it needed? Link the issue. -->

## How it was tested

<!-- Tests added, manual steps, screenshots for UI changes (web at desktop and 400px). -->

## Checklist

- [ ] `cargo fmt`, `cargo clippy -- -D warnings` and `cargo test` pass
- [ ] `bun run typecheck`, `bun run lint` and `bun run build` pass (if `web/` changed)
- [ ] Docs updated (crate docs, ARCHITECTURE, ADR if a decision changed)
- [ ] Keeps Soulseek first, streaming opt-in, and review before import
